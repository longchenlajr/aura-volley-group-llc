export const dynamic = "force-dynamic";
export const metadata = { title: { absolute: "Live | Long Volleyball" } };

export default function LiveLayout({ children }: { children: React.ReactNode }) {
  return children;
}
