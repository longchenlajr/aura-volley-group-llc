export const metadata = { title: { absolute: "Bracket Score Entry | Long Volleyball" } };

export default function BracketScoreLayout({ children }: { children: React.ReactNode }) {
  return children;
}
